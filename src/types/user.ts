export default interface User {
    id: string;
    email: string;
    firstName: string;
    lastName: string;
    roles: string;
    emailNotification: boolean;
    createdAt: string;
    updatedAt: string;
}